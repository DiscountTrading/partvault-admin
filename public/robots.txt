# Admin app is crawlable. The superadmin panel has no crawlable URL — it's a
# URL fragment (admin.partvault.app/#superadmin) which search engines ignore,
# and it has no link anywhere — so there's nothing to index or disallow.
User-agent: *
Allow: /
